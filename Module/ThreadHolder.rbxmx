<roblox xmlns:xmime="http://www.w3.org/2005/05/xmlmime" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://www.roblox.com/roblox.xsd" version="4">
	<Meta name="ExplicitAutoJoints">true</Meta>
	<External>null</External>
	<External>nil</External>
	<Item class="ModuleScript" referent="RBX9ED4CB4A198C4A98BED8C5539CFE443C">
		<Properties>
			<Content name="LinkedSource"><null></null></Content>
			<ProtectedString name="Source"><![CDATA[------------------------------------------------------
---Services/Modules-----------------------------------
------------------------------------------------------

local HTTP = game:GetService('HttpService')

local te = require(script:WaitForChild('Type'))

------------------------------------------------------
---Assign---------------------------------------------
------------------------------------------------------

local api: te.api = {}

local Overwrite
local UpdateTime

local addWarn = ", you can disable this warn and overwrite thread by enable Overwrite_Old_Thread attribute"

------------------------------------------------------
---Do-------------------------------------------------
------------------------------------------------------

do
	Overwrite = script:GetAttribute('Overwrite_Old_Thread')
	UpdateTime = math.abs(script:GetAttribute('AutoClear_Update_Time'))
	
	if UpdateTime < 1 then error(`{UpdateTime} is lower than 1, it may cause excessive load`) end
end

------------------------------------------------------
---LoclaFn--------------------------------------------
------------------------------------------------------

local function autoClear(self)
	return task.spawn(function(self: te.handlerPrivate)
		while task.wait(UpdateTime) do
			for i,v in self.rawCache do
				if typeof(v) ~= "thread" then
					warn(`{i} is not a thread`, debug.traceback())
					self.rawCache[i] = nil
					continue
				end
				if coroutine.status(v) == "dead" then
					self.rawCache[i] = nil
				end
			end
		end
	end, self)
end

------------------------------------------------------
---Class----------------------------------------------
------------------------------------------------------

function api.new(id: string?, disableAutoClear: boolean?)
	local self = setmetatable({}, {
		__index = function(self, key)
			if key == "isAutoClear" then
				return not not self.autoClear
			end
			
			if key == "isEmpty" then
				return not next(self.rawCache)
			end
			
			return rawget(self, key)
		end,
	})
	self.id = id or HTTP:GenerateGUID(false)
	
	self._ch_mt = {
		__newindex = function(tbl, key, value)
			local oldKey = self.rawCache[key]
			
			if typeof(oldKey) == "thread" and coroutine.status(oldKey) ~= "dead" then
				coroutine.close(oldKey)
			end

			if not (typeof(value) == "thread" or value == nil) then warn(value, " is not a thread or nil") return end
			self.rawCache[key] = value
		end,
		__index = function(tbl, key)
			return self.rawCache[key]
		end,
	}
	
	self.rawCache = {}
	self.cache = setmetatable({}, self._ch_mt)
	self.autoClear = not disableAutoClear and autoClear(self) or false
	
	function self:AddThread(id: string, thread: thread)
		if not Overwrite and self.rawCache[id] then warn(`{id} already exists in cache{addWarn}`) return end
		self.cache[id] = thread
		return self
	end
	
	function self:GetAutoCache(reMeta: boolean?)
		if reMeta then
			return setmetatable(self.cache, self._ch_mt)
		end
		return self.cache
	end
	
	function self:RemoveThread(id: string)
		self.rawCache[id] = nil
		return self
	end
	
	function self:CancelThread(id: string)
		self.cache[id] = nil
		return self
	end
	
	function self:CancelAllThreads()
		for i,_ in self.rawCache do
			self.cache[i] = nil
		end
		return self
	end
	
	function self:Destroy()
		if self.autoClear then
			if coroutine.status(self.autoClear) ~= "dead" then
				coroutine.close(self.autoClear)
			end
		end
		self:CancelAllThreads()
		setmetatable(self.cache, nil)
		setmetatable(self, nil)
		table.clear(self)
	end
	
	return self
end


return api]]></ProtectedString>
			<string name="ScriptGuid">{507A6965-E768-4AA3-BC9B-AE43E6165244}</string>
			<BinaryString name="AttributesSerialize"><![CDATA[AgAAABUAAABBdXRvQ2xlYXJfVXBkYXRlX1RpbWUGAAAAAAAAXkAUAAAAT3ZlcndyaXRlX09s
ZF9UaHJlYWQDAQ==]]></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<string name="Name">ThreadHolder</string>
			<int64 name="SourceAssetId">-1</int64>
			<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
		</Properties>
		<Item class="ModuleScript" referent="RBXB31AA84112FD492087B0D0B89373F958">
			<Properties>
				<Content name="LinkedSource"><null></null></Content>
				<ProtectedString name="Source"><![CDATA[local te = {}

export type api = {
	new: (id: string?, disableAutoClear: boolean?) -> handlerPublic,
}

export type handlerPublic = {
	read isAutoClear: boolean,
	read isEmpty: boolean,
	
	AddThread: (self: handlerPublic, id: string, thread: thread) -> handlerPublic,
	GetAutoCache: (self: handlerPublic, reMeta: boolean?) -> {[string]: thread},
	RemoveThread: (self: handlerPublic, id: string?) -> handlerPublic,
	CancelThread: (self: handlerPublic, id: string?) -> handlerPublic,
	CancelAllThreads: (self: handlerPublic) -> handlerPublic,
	Destroy: (self: handlerPublic) -> ()
}

export type handlerPrivate = handlerPublic & {
	rawCache: {[string]: thread},
	cache: {[string]: thread}
}

return te]]></ProtectedString>
				<string name="ScriptGuid">{730E2664-77BF-4795-90C4-33B59765F61B}</string>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<string name="Name">Type</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			</Properties>
		</Item>
		<Item class="LocalScript" referent="RBX8EBDC18CCF254501BE5D09E95344D279">
			<Properties>
				<ProtectedString name="Source">-- https://github.com/m1rrun/Maid_Luau/blob/main/README.md</ProtectedString>
				<bool name="Disabled">true</bool>
				<Content name="LinkedSource"><null></null></Content>
				<token name="RunContext">0</token>
				<string name="ScriptGuid">{728345A1-1D11-41F5-9763-10EEB8634FF8}</string>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<string name="Name">DOCS</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			</Properties>
		</Item>
		<Item class="PackageLink" referent="RBXF9CDB9E6205848FD81386DC11B61FDCB">
			<Properties>
				<bool name="AutoUpdate">false</bool>
				<string name="DefaultName"></string>
				<int name="ModifiedState">-1</int>
				<Content name="PackageIdSerialize"><url>rbxassetid://104061698386399</url></Content>
				<BinaryString name="SerializedDefaultAttributes"></BinaryString>
				<int64 name="VersionIdSerialize">1</int64>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<string name="Name">PackageLink</string>
				<int64 name="SourceAssetId">-1</int64>
				<SharedString name="Tags">yuZpQdnvvUBOTYh1jqZ2cA==</SharedString>
			</Properties>
		</Item>
	</Item>
	<SharedStrings>
		<SharedString md5="yuZpQdnvvUBOTYh1jqZ2cA=="></SharedString>
	</SharedStrings>
</roblox>